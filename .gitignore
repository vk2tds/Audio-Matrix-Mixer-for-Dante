.venv/
__pycache__/
*.pyc
.DS_Store
logs/

# Vendored Font Awesome Pro kit (licensed, not committed — see setup note in
# README). Present locally after extracting the user's own FA Pro download;
# the app serves it from disk regardless of git tracking.
#static/vendor/
